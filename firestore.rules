rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // This app reads/writes Firestore through trusted Cloud Functions only.
    // Keep direct client access disabled by default.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
